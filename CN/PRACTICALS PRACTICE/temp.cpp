#include<bits/stdc++.h>
using namespace std;
// int xorr(data,crc){

// }
int main(){
    string data,crc,encoded="";
    cin>>data;
    cin>>crc;
    cout<<data.length();
    encoded+=data;
    cout<<encoded[1];
    if(encoded[1]=='r'){
        cout<<"dd";
    }
    // for(i=encoded[i])
}